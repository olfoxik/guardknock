Сервер, слушает указанный в config.ini порт
и ожидает авторизацию указанных там пользователей.
После успешной авторизации пользователй выдает строчку
для port knocking которая указывается в config.ini
